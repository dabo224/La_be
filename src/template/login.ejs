
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion - Université de Labé</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Montserrat', Arial, sans-serif;
            background: linear-gradient(135deg, #f0f2f5 60%, #41AB55 100%);
            min-height: 100vh;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .auth-wrapper {
            display: flex;
            flex-direction: column;
            align-items: center;
            width: 100%;
            max-width: 400px;
            padding: 32px 16px;
        }
        .logo {
            width: 80px;
            height: 80px;
            margin-bottom: 16px;
            border-radius: 50%;
            box-shadow: 0 4px 24px rgba(65,171,85,0.15);
            background: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: transform 0.3s;
        }
        .logo img {
            width: 70px;
            height: 70px;
        }
        .logo:hover {
            transform: scale(1.08) rotate(-3deg);
        }
        .dynamic-title {
            font-size: 1.3rem;
            font-weight: 700;
            color: #1877f2;
            margin-bottom: 8px;
            text-align: center;
        }
        .dynamic-desc {
            font-size: 1.1rem;
            font-weight: 500;
            color: #333;
            margin-bottom: 24px;
            text-align: center;
        }
        #dynamic-text {
            font-weight: 700;
            transition: color 0.3s;
        }
        .cursor {
            display: inline-block;
            width: 2px;
            height: 1.2em;
            background: #222;
            vertical-align: middle;
            animation: blink 1s infinite;
        }
        @keyframes blink {
            0%, 50% { opacity: 1; }
            50.01%, 100% { opacity: 0; }
        }
        .auth-card {
            background: #fff;
            border-radius: 16px;
            box-shadow: 0 8px 32px rgba(24,119,242,0.08), 0 1.5px 8px rgba(65,171,85,0.08);
            padding: 32px 24px 24px 24px;
            width: 100%;
            max-width: 370px;
            transition: box-shadow 0.3s, transform 0.3s;
        }
        .auth-card:hover {
            box-shadow: 0 12px 40px rgba(24,119,242,0.15), 0 2px 12px rgba(65,171,85,0.12);
            transform: translateY(-2px) scale(1.01);
        }
        .auth-card h2 {
            font-size: 1.5rem;
            font-weight: 700;
            color: #1877f2;
            margin-bottom: 18px;
            text-align: center;
        }
        form {
            display: flex;
            flex-direction: column;
            gap: 16px;
        }
        .form-group {
            display: flex;
            flex-direction: column;
        }
        label {
            font-size: 0.98rem;
            color: #555;
            margin-bottom: 6px;
            font-weight: 500;
        }
        input[type="email"], input[type="password"] {
            padding: 10px 12px;
            font-size: 1rem;
            border: 1.5px solid #e0e0e0;
            border-radius: 6px;
            outline: none;
            transition: border-color 0.2s;
        }
        input[type="email"]:focus, input[type="password"]:focus {
            border-color: #41AB55;
        }
        button[type="submit"] {
            padding: 12px;
            font-size: 1.08rem;
            font-weight: 700;
            background: linear-gradient(90deg, #1877f2 60%, #41AB55 100%);
            color: #fff;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            box-shadow: 0 2px 8px rgba(24,119,242,0.08);
            transition: background 0.2s, box-shadow 0.2s;
        }
        button[type="submit"]:hover {
            background: linear-gradient(90deg, #41AB55 60%, #1877f2 100%);
            box-shadow: 0 4px 16px rgba(65,171,85,0.12);
        }
        .forgot-password, .signup-link {
            text-align: center;
            margin-top: 8px;
        }
        .forgot-password a {
            color: #1877f2;
            text-decoration: none;
            font-size: 0.98rem;
        }
        .forgot-password a:hover {
            text-decoration: underline;
        }
        .signup-link a {
            color: #41AB55;
            text-decoration: none;
            font-weight: 700;
            font-size: 1.05rem;
        }
        .signup-link a:hover {
            text-decoration: underline;
        }
        #error {
            color: #d32f2f;
            font-size: 1rem;
            text-align: center;
            margin-top: 10px;
            min-height: 22px;
        }
        @media (max-width: 600px) {
            .auth-wrapper {
                max-width: 100vw;
                padding: 12px 4px;
            }
            .auth-card {
                padding: 18px 8px 16px 8px;
                max-width: 98vw;
            }
            .logo {
                width: 60px;
                height: 60px;
            }
            .logo img {
                width: 50px;
                height: 50px;
            }
        }
    </style>
</head>
<body>
    <div class="auth-wrapper">
        <div class="logo">
            <img src="/static/images/logo.png" alt="Logo Université de Labé">
        </div>
        <div class="dynamic-title">Université de Labé</div>
        <div class="dynamic-desc">
            C'est <span id="dynamic-text"></span><span class="cursor"></span>
        </div>
        <div class="auth-card">
            <h2>Connexion</h2>
            <form id="login-form" autocomplete="on">
                <div class="form-group">
                    <label for="email">Adresse e-mail</label>
                    <input type="email" id="email" name="email" placeholder="Adresse e-mail" required autocomplete="email">
                </div>
                <div class="form-group">
                    <label for="password">Mot de passe</label>
                    <input type="password" id="password" name="password" placeholder="Mot de passe" required autocomplete="current-password">
                </div>
                <button type="submit">Se connecter</button>
                <div class="forgot-password">
                    <a href="#">Mot de passe oublié ?</a>
                </div>
                <div class="signup-link">
                    <a href="/inscription">Créer un nouveau compte</a>
                </div>
            </form>
            <div id="error"></div>
        </div>
    </div>
    <script>
        // Texte dynamique animé
        const words = ["la famille", "l'excellence", "la solidarité", "l'innovation", "le partage"];
        const colors = ["#EA9338", "#FECB38", "#41AB55", "#1877f2", "#d32f2f"];
        let wordIndex = 0;
        let charIndex = 0;
        let currentWord = "";
        let isDeleting = false;
        const dynamicText = document.getElementById("dynamic-text");

        function type() {
            if (isDeleting) {
                currentWord = words[wordIndex].substring(0, charIndex--);
            } else {
                currentWord = words[wordIndex].substring(0, charIndex++);
            }
            dynamicText.textContent = currentWord;
            dynamicText.style.color = colors[wordIndex];
            if (!isDeleting && charIndex > words[wordIndex].length) {
                isDeleting = true;
                setTimeout(type, 1800);
            } else if (isDeleting && charIndex < 0) {
                isDeleting = false;
                wordIndex = (wordIndex + 1) % words.length;
                setTimeout(type, 600);
            } else {
                setTimeout(type, isDeleting ? 40 : 90);
            }
        }
        document.addEventListener("DOMContentLoaded", () => {
            setTimeout(type, 400);
        });

        // Gestion du formulaire
        document.getElementById("login-form").addEventListener("submit", async (e) => {
            e.preventDefault();
            const email = e.target.email.value.trim();
            const password = e.target.password.value;
            const errorDiv = document.getElementById('error');
            errorDiv.textContent = '';
            try {
                const res = await fetch("/login", {
                    method: "POST",
                    headers: { "Content-Type": "application/json" },
                    body: JSON.stringify({ email, password })
                });
                const data = await res.json();
                if (data.error) {
                    errorDiv.textContent = data.error;
                } else {
                    localStorage.setItem('token', data.token);
                    window.location.href = "/home";
                }
            } catch (err) {
                errorDiv.textContent = "Erreur de connexion. Veuillez réessayer.";
            }
        });
    </script>
</body>
</html>